# The Bombay Forum — Environment Configuration
PORT=8080
NODE_ENV=development
SITE_URL=https://thebombayforum.com

# MongoDB Atlas URI (Optional — automatically falls back to local JSON datastore if omitted)
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.oyrf2y7.mongodb.net/the_bombay_forum?retryWrites=true&w=majority

# JWT Authentication Secret
JWT_SECRET=tbf-editorial-secret-key-2026-mumbai-982347

# LLM Rewriting APIs (Optional — features high-quality heuristic rewrite fallback if omitted)
GEMINI_API_KEY=
ANTHROPIC_API_KEY=

# Automation & Distribution Webhooks (n8n)
PUBLISH_WEBHOOK_URL=

# Instagram Graph API Token (For social distribution)
INSTAGRAM_ACCESS_TOKEN=
